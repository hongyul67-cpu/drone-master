# 드론 제작 마스터

특성화고 항공드론과 「드론 제작」 교과용 학습 도구. 정적 HTML만으로 동작하며 GitHub Pages로 배포한다.

**배포 주소** — https://hongyul67-cpu.github.io/drone-master/

## 구성

| 파일 | 내용 | 근거 |
|---|---|---|
| `index.html` | 허브. 카드 링크에 `?rc=`를 자동으로 물려 준다 | — |
| **만들기 게임** | | |
| `build-sim-pixhawk.html` | 7단계: 부품 도감 → 프레임·모터 조립 → 픽스호크 결선 → 프로펠러 → 조종기 세팅 → 미션 플래너 보정 → 가상 시험비행 | 교과서 Ⅳ영역 (p120~166) |
| `build-sim-arduino.html` | 6단계: 부품 도감 → FC 기판 핀 결선 → 3D 출력 설정 → 모터·ESC 조립 → MultiWii 세팅 → ESC 보정·비행모드·안전점검 | 교과서 Ⅲ영역 (p84~119) |
| `build-mission-device.html` | 에어 드롭 장치 · 2축 짐벌: 모델링 순서 → 출력 설정 → 서보 제어로 투하 → 조종기 연결 → 출력 과정 | 교과서 Ⅴ영역 (p168~195) |
| **이론 6차시** | | |
| `01-drone-overview.html` | 용어(UAV·UAS·RPAS) · 종류 분류 · 역사 기체 카드 | 1차시 학습지 + p14~19 |
| `02-flight-principle.html` | 네 가지 힘 도해 + 조종기 4용어를 **직접 조종하며** 익히는 실습 | 2차시 학습지 + p22~25 |
| `03-drone-structure.html` | 프레임·FC·ESC·모터·배터리 (배우기 → 조립게임 → 센서게임 → 학습지) | 3차시 학습지 + p27~31 |
| `04-drone-law.html` | 초경량비행장치 · 조종자 증명 1~4종 · 준수사항 | 4차시 학습지 + p33~37 |
| `05-battery-sensor-check.html` | 리포 전압 · 센서 5종 · 안전성인증검사 4종 · 벌칙 | 5차시 학습지 |
| `06-registration-airspace.html` | 신고번호 C/N·M/H/P/S · 공역 A~G · 관제/비관제/통제/주의 | 6차시 학습지 |
| **보고서** | | |
| `report.html` | 게임 기록 자동 유입 · 자기평가 루브릭 · 서술형 4문항 · 사진 첨부 · A4 인쇄/PDF | 교과서 「실습 점검」 루브릭 |

## 공용 파일

| 파일 | 역할 |
|---|---|
| `drone-anim.js` | 부품이 하나씩 붙는 SVG 드론. `DroneAnim.mount(el,{layout})` → `part(name,on)` / `spin(0~2)` / `takeoff()`. layout이 `pixhawk`면 1·2번이 CCW, `multiwii`면 1·3번이 CW로 프로펠러가 반대로 돈다. |
| `lesson.js` + `lesson.css` | 이론 차시 공용 엔진. 데이터만 넘기면 화면·게임·채점·제출이 만들어진다. 화면 종류: `cards`(배우기) · `sort`(분류) · `match`(매칭) · `pick`(맞는 것 고르기) · `seq`(순서) · `custom`(직접 render) · `quiz`(자동 채점). 학습지 보기는 매번 섞인다. |

## 도구 사이의 연결

- 게임을 끝내면 결과가 `localStorage`에 저장된다 (`drone_build_pixhawk`, `drone_build_arduino`).
- 보고서가 그 값을 읽어 단계별 실수 횟수까지 자동으로 채운다. 게임 없이 보고서만 써도 된다.
- 게임 결과 화면의 「보고서 쓰러 가기」는 `?rc=`를 그대로 넘긴다.

## 결과 수집

`https://hongyul67-cpu.github.io/links/collector.js` 위젯을 쓴다.
교사가 `?rc=<본인 Apps Script exec 주소>`를 붙인 링크로 나눠 줄 때만 제출 버튼이 나타난다.
보고서는 서술형 4문항 본문을 `wrong` 필드에 담아 보내므로 **시트에서 학생 글을 바로 읽을 수 있다** (사진은 전송하지 않음 — 인쇄본으로 제출).

## 알아 둘 것

- 교과서 89쪽 결선표에는 `SDA=A5, SCL=A4`로 적혀 있으나 88쪽 설명과 아두이노 나노 실제 핀 배치는 `A4=SDA, A5=SCL`이다. 게임은 실제 배치를 정답으로 하고 화면에 경고를 띄운다.
- 모터 번호 규칙이 FC마다 다르다. 픽스호크(ArduPilot) 쿼드X는 1=앞·오른(CCW), 2=뒤·왼(CCW), 3=앞·왼(CW), 4=뒤·오른(CW). MultiWii 방식은 1·3=CW, 2·4=CCW. 두 게임이 서로 다른 것이 맞으며, 아두이노 게임에서 이 차이를 경고로 설명한다.
- 3D 출력 설정이 단원마다 다르다. 드론 **프레임**(Ⅲ영역)은 서포트를 「베드에 닿는 곳만」으로, **에어 드롭 장치**(Ⅴ영역)는 형상이 복잡해 「모든 곳」으로 생성한다. 두 게임이 서로 다른 것이 맞다.
- 벌칙 금액은 법 개정으로 바뀔 수 있다. 5차시에 그 안내를 넣어 두었으니, 시험 직전에는 최신 항공안전법을 확인할 것.
- `03-drone-structure.html`만 다른 세션에서 만들어져 `lesson.js` 엔진을 쓰지 않는다(자체 HTML). 동작에는 문제가 없다.
